<Controls.Common:BasicWindow 
    x:Class="Imagin.Controls.Extended.GradientDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:Imagin.Controls.Extended"
    xmlns:Controls.Common="clr-namespace:Imagin.Controls.Common;assembly=Imagin.Controls.Common"
    Height="380" 
    Title="Gradient Dialog" 
    Type="Tool"
    Width="450"
    WindowStartupLocation="CenterScreen"
    x:Name="PART_GradientDialog">
    <Grid DataContext="{Binding ElementName=PART_GradientDialog}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <local:GradientEditor x:Name="PART_GradientEditor" Gradient="{Binding Gradient}"/>
        <Controls.Common:Spacer 
            Grid.Row="1" 
            Orientation="Horizontal" 
            Spacing="3,0,0,0"
            HorizontalAlignment="Right"
            Margin="0,10,0,0">
            <Button 
                Content="Save" 
                IsDefault="True" 
                Click="OnSave"/>
            <Button 
                Content="Cancel" 
                IsCancel="True" 
                Click="OnCancel"/>
        </Controls.Common:Spacer>
    </Grid>
</Controls.Common:BasicWindow>
