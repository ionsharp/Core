<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Imagin.Controls.Common">
    <Style TargetType="{x:Type local:Line}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:Line}">
                    <Rectangle x:Name="PART_Rectangle" Fill="{TemplateBinding Stroke}"/>
                    <ControlTemplate.Triggers>
                        <Trigger Property="Orientation" Value="Horizontal">
                            <Setter TargetName="PART_Rectangle" Property="Width" Value="Auto"/>
                            <Setter TargetName="PART_Rectangle" Property="Height" Value="{Binding Thickness, RelativeSource={RelativeSource TemplatedParent}}"/>
                        </Trigger>
                        <Trigger Property="Orientation" Value="Vertical">
                            <Setter TargetName="PART_Rectangle" Property="Width" Value="{Binding Thickness, RelativeSource={RelativeSource TemplatedParent}}"/>
                            <Setter TargetName="PART_Rectangle" Property="Height" Value="Auto"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>