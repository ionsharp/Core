<local:CheckableDatePicker 
    x:Class="Imagin.Controls.Common.CheckableWeekDays"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:local="clr-namespace:Imagin.Controls.Common"
    xmlns:Common.Data.Converters="clr-namespace:Imagin.Common.Data.Converters;assembly=Imagin.Common"
    Height="50"
    Width="350"
    x:Name="PART_CheckableWeekDays">
    <local:CheckableDatePicker.Resources>
        <Common.Data.Converters:SubstringConverter x:Key="SubstringConverter"/>
    </local:CheckableDatePicker.Resources>
    <local:CheckableDatePicker.ItemTemplate>
        <DataTemplate>
            <ToggleButton 
                BorderThickness="0" 
                Height="50" 
                IsChecked="{Binding IsChecked}"
                Width="50">
                <TextBlock Text="{Binding Mode=OneWay, Converter={StaticResource SubstringConverter}, ConverterParameter=3}"/>
            </ToggleButton>
        </DataTemplate>
    </local:CheckableDatePicker.ItemTemplate>
    <local:CheckableDatePicker.ItemsPanel>
        <ItemsPanelTemplate>
            <UniformGrid Columns="7"/>
        </ItemsPanelTemplate>
    </local:CheckableDatePicker.ItemsPanel>
</local:CheckableDatePicker>
