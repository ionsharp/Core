<local:CheckableDatePicker 
    x:Class="Imagin.Controls.Common.CheckableDays"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:Imagin.Controls.Common"
    Height="250"
    Width="350"
    x:Name="PART_CheckableCalendar">
    <local:CheckableDatePicker.ItemTemplate>
        <DataTemplate>
            <ToggleButton
                BorderThickness="0"
                Focusable="{Binding IsFocusable, ElementName=PART_CheckableDays}"
                Height="50"
                IsChecked="{Binding IsChecked}"
                Width="50">
                <TextBlock Text="{Binding Mode=OneWay}"/>
            </ToggleButton>
        </DataTemplate>
    </local:CheckableDatePicker.ItemTemplate>
    <local:CheckableDatePicker.ItemsPanel>
        <ItemsPanelTemplate>
            <UniformGrid Columns="7" Rows="5"/>
        </ItemsPanelTemplate>
    </local:CheckableDatePicker.ItemsPanel>
</local:CheckableDatePicker>
