<ContentControl
    x:Class="Imagin.Controls.Common.FlagCheckView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Imagin.Controls.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="PART_FlagCheckView">
    <ItemsControl
        DataContext="{Binding ElementName=PART_FlagCheckView, Mode=OneWay}"
        ItemsPanel="{Binding ItemsPanel, Mode=OneWay}"
        ItemsSource="{Binding Items, Mode=OneWay}"
        ItemTemplate="{Binding ItemTemplate, Mode=OneWay}"/>
</ContentControl>
