<UserControl x:ConnectionId='1'
    x:Class="Imagin.Controls.Common.DateTimePicker"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Imagin.Controls.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Core="using:Microsoft.Xaml.Interactions.Core"
    xmlns:Data="using:Imagin.Common.Data"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    x:Name="PART_DateTimePicker">
    <UserControl.Resources>
        <Data:DateTimeToDateTimeOffsetConverter x:Key="DateTimeToDateTimeOffsetConverter"/>
    </UserControl.Resources>
    <StackPanel 
        DataContext="{Binding ElementName=PART_DateTimePicker}"
        Orientation="{Binding Orientation}">
        <DatePicker
            Date="{Binding DateTime, Converter={StaticResource DateTimeToDateTimeOffsetConverter}, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>
        <TimePicker x:ConnectionId='2'
            Grid.Column="1"
            x:Name="PART_TimePicker"
                                       
            Visibility="{Binding TimeVisibility, Mode=OneWay}">
            <i:Interaction.Behaviors>
                <Core:DataTriggerBehavior Binding="{Binding Orientation, Mode=OneWay}" Value="Horizontal">
                    <Core:ChangePropertyAction PropertyName="Margin" Value="10,0,0,0" />
                </Core:DataTriggerBehavior>
                <Core:DataTriggerBehavior Binding="{Binding Orientation, Mode=OneWay}" Value="Vertical">
                    <Core:ChangePropertyAction PropertyName="Margin" Value="0,10,0,0" />
                </Core:DataTriggerBehavior>
            </i:Interaction.Behaviors>
        </TimePicker>
    </StackPanel>
</UserControl>

